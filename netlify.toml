[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# React Router / SPA deep links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
